# Bili Digest Plus 哔哩精读增强版

这是供本机测试的二开增强版，与原版扩展使用不同的显示名称。

本副本包含当前已完成的改进：

- AI 传输与 B站权限边界加固；
- 阅读、界面、等宽三类字体预设（含 50+ 项固定安全中英文字体），并可为字幕、概览、笔记、对话、设置五个功能区分别选择字体与 12–50px 字号；顶部五项标签栏共享界面字体和同一个导航字号；未安装字体会自动回退；
- 两个设置入口都支持字体搜索（中英文子串、空格/连字符容错），并提供五区即时预览、恢复默认和点击保存；
- 点击设置页“读取本机字体”后，由 Chrome 询问 Local Font Access（`queryLocalFonts()`）权限；字体名称只在本机使用，不上传，也不会读取字体文件内容；
- 设置页与侧边栏设置使用完全一致的排版控件，并可切换是否显示视频页“标记”按钮（默认显示，保存后即时同步到已打开的视频页）以及是否显示顶部品牌文字（默认显示）。

## 在 Chrome 中试用

1. 打开 `chrome://extensions`；
2. 开启右上角“开发者模式”；
3. 点击“加载已解压的扩展程序”；
4. 选择本文件所在的 `bili-digest-plus` 文件夹；
5. 扩展列表中应显示“Bili Digest Plus 哔哩精读增强版”。

它和原版是两个不同的扩展安装实例，设置与本地数据不会自动互通。

原项目许可证见 `LICENSE`，原项目说明见 `README.md`。
